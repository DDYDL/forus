<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.hospital_qna">
	<insert id="insertQuestion" parameterType="dto.Hospital_qna">
		INSERT INTO hospital_qna (user_id, h_id, q_title, q_time)
		VALUES (#{user_id}, #{h_id}, #{q_title}, NOW());
	</insert>

	<select id="selectQnaList" parameterType="dto.Hospital_qna" resultType="dto.Hospital_qna">

		SELECT * FROM hospital_qna WHERE h_id = #{h_id} ORDER BY q_time DESC;
	</select>

</mapper>